<%= form_with model: [post, comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '3', placeholder: "コメントをここに ※50文字以内", class: "w-100 comment-text_area" %>
  <%= f.submit "送信する", class: 'btn btn-primary pull-right' %>
<% end %>